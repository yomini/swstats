import matplotlib.pyplot as plt
import seaborn as sns
from pandas import Series, DataFrame
import json

# lcd = {}
# with open('../../../data/lang_country_demand.json', 'r') as r:
#     lcd = json.load(r)
# print(lcd)
# lcd_key = list(lcd['Java'].keys())
# lcdDF = DataFrame(lcd)
# print(lcdDF)
#
# sns.set()
#
# f, ax = plt.subplots(figsize=(9, 6))
# # 세로 - 언어, 가로 - 국가
# sns.heatmap(lcdDF, annot=True, fmt='d', linewidths=.6, ax=ax)
#
# plt.show()

